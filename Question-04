 //Write a JavaScript program to compute the sum of the two given integers. If the two values are the same, then print triple their sum.
 
function sumTriple(num1, num2) {
    if (num1 == num2) {
      return 3 * (num1 + num2);
    } else {
      return (num1 + num2);
    }
  }
